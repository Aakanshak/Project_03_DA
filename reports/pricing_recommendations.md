# Pricing and Promotion Recommendations

Result generation is pending because the required feature, elasticity, and
promotion-impact datasets are not present in this workspace.

After generating those inputs, run:

```powershell
python src/scenario_simulator.py
python src/recommend.py
```

The second command will replace this note with segment-level recommendations,
projected revenue changes, and expected gross-margin lift.

